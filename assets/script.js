/* const firstName = prompt("Siapa namamu?");
const language = prompt("Bisa berbahasa daerah ?");
 
const user = {
   name: {
       first: firstName,
   },
   language: language
};
 
if (user.language === "jawa") {
   alert("Matur nuwun " + user.name.first + "!");
} else if (user.language === "sunda") {
   alert("Hatur nuhun " + user.name.first + "!");
} else if (user.language === "batak") {
   alert("Hoorasss " + user.name.first + "!");
} else {
   alert("Terimakasih " + user.name.first + "!");
}
*/

alert("Terimakasih Sudah Berkunjung");